ptp4l_config_dir: /etc/linuxptp
software
syslog_server_ip: "192.0.2.5"     # Replace with your actual syslog server IP
syslog_server_port: 514           # Default TCP port
syslog_transport: "tcp"           # 'tcp' or 'udp'

